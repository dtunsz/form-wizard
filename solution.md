```
This application is built with vue 3 and Typescript.

Choices of technology used is based on recent version of vue application and Typescript was chosen to concretize data types specification all over the application

Sub component such as `Button` was created for reusability

Data created within the application is shared across components via router-view one of the easiest way to share data without the use of state management

```